import { STORE_CATEGORIES } from "./storeCategories";
import type { Category, Product } from "../types";

/** Catálogo vacío por defecto; categorías según pestañas del Excel. */
export const DEFAULT_CATEGORIES: Category[] = STORE_CATEGORIES;
export const MOCK_PRODUCTS: Product[] = [];
